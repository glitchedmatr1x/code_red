#pragma once

/* Minimal Code RED proof natives for Script Compile Lab validation.
   These declarations are source-proof shims. Final SC-CL native hashes should be supplied by the full native database/toolchain when compiling real outputs. */

void _CLEAR_PRINTS(void);
void _PRINT_SUBTITLE(const char* text, int duration, int p2, int p3, int p4);
int _IS_KEY_PRESSED(int key);
Actor GET_PLAYER_ACTOR(int playerIndex);
int _IS_LAYOUT_VALID(Layout layout);
Layout CREATE_LAYOUT(const char* name);
Actor FIND_ACTOR_IN_LAYOUT(Layout layout, const char* name);
void DESTROY_ACTOR(Actor actor);
void STREAMING_REQUEST_ACTOR(int actorModel, int priority, int flags);
Actor CREATE_ACTOR_IN_LAYOUT(Layout layout, const char* name, int actorModel, float x, float y, float z, float heading);
int IS_ACTOR_VALID(Actor actor);
int IS_ACTOR_VEHICLE(Actor actor);
void ENABLE_VEHICLE_SEAT(Actor vehicle, int seat, int enabled);
void SET_VEHICLE_ALLOWED_TO_DRIVE(Actor vehicle, int enabled);
void SET_VEHICLE_ENGINE_RUNNING(Actor vehicle, int running);
void VEHICLE_SET_HANDBRAKE(Actor vehicle, int enabled);
void START_VEHICLE(Actor vehicle);
void SET_ACTOR_IN_VEHICLE(Actor actor, Actor vehicle, int seat);
void SET_ACTOR_MAX_SPEED(Actor actor, float speed);
void SET_ACTOR_MAX_SPEED_ABSOLUTE(Actor actor, float speed);
void SET_ACTOR_SPEED(Actor actor, float speed);
void ADD_PERSISTENT_SCRIPT(ScriptId scriptId);
ScriptId _GET_ID_OF_THIS_SCRIPT(void);
void WAIT(int ms);
