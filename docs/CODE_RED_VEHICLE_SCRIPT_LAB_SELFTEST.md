# Self-test

Verified locally with synthetic WSC-like binary files containing known vehicle references.

Commands exercised:

- `status`
- `scan-file`
- `compare`
- `target-map`
- `make-asi-scaffold`

The scan detected direct vehicle/script references and generated CSV/JSON/Markdown reports.
